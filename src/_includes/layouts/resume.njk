---
layout: base
---

{%- from "components/resume.njk" import resume -%}

<main>
  <h1>{{ title }}</h1>
  {{ content | safe }}
  {%- for a in authors -%}
    {% if a.name === "Harvey A. Ramer"%}
      {% if a.resume %}
        {{resume(a, "h1")}}
      {% endif %}
    {% endif %}
  {%- endfor -%}
  {% include "../partials/share.njk" %}
</main>